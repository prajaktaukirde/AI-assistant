# Server
PORT=4000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:3000

# MongoDB (Atlas connection string for production)
MONGO_URI=mongodb://localhost:27017/vedaai

# Redis (Upstash for production: rediss://default:<password>@<host>:<port>)
REDIS_URL=redis://localhost:6379

# Google Gemini (free key from https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=AIzaSyARsJKiit78xv9ni5Pz3G9AJ76vVj9qFhw
GEMINI_MODEL=gemini-2.5-flash

# When true, runs the BullMQ worker inside the same process as the API.
# Useful for free Render plans that only allow one service.
RUN_WORKER_INLINE=true
